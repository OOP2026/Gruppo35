package gui;

import com.intellij.uiDesigner.core.GridLayoutManager;

import javax.swing.*;
import java.awt.*;
import java.awt.event.ActionListener;

/**
 * Pannello Swing utilizzato dal docente per inserire una richiesta
 * di spostamento lezione.
 * Il pannello permette di indicare il nuovo giorno, la nuova ora di inizio
 * e la nuova ora di fine proposti per la lezione.
 */
public class GUIrichiestaspostamentolezione extends JPanel {
    private final JTextField data;
    private final JTextField orainizio;
    private final JTextField orafine;
    private final JButton inviarichiesta;
    private final JButton annulla;
    private JPanel mainPanel;

    /**
     * Crea il pannello per la richiesta di spostamento lezione.
     * Inizializza i campi di input e i pulsanti per inviare o annullare
     * la richiesta.
     */
    public GUIrichiestaspostamentolezione() {
        $$$setupUI$$$();
        setLayout(new GridLayout(4, 2, 10, 10));
        add(new JLabel("Data (AAAA-MM-GG):"));
        data = new JTextField();
        add(data);
        add(new JLabel("Ora Inizio (HH:MM):"));
        orainizio = new JTextField();
        add(orainizio);
        add(new JLabel("Ora Fine (HH:MM):"));
        orafine = new JTextField();
        add(orafine);
        inviarichiesta = new JButton("Invia Richiesta");
        annulla = new JButton("Annulla");
        add(inviarichiesta);
        add(annulla);
    }

    /**
     * Restituisce il valore inserito nel campo della data proposta.
     *
     * @return data inserita dall'utente
     */
    public String getDataInput() {
        return data.getText();
    }

    /**
     * Restituisce il valore inserito nel campo dell'ora di inizio proposta.
     *
     * @return ora di inizio inserita dall'utente
     */
    public String getOraInizioInput() {
        return orainizio.getText();
    }

    /**
     * Restituisce il valore inserito nel campo dell'ora di fine proposta.
     *
     * @return ora di fine inserita dall'utente
     */
    public String getOraFineInput() {
        return orafine.getText();
    }

    /**
     * Collega un listener al pulsante per inviare la richiesta.
     * Il listener viene normalmente fornito dal controller.
     *
     * @param listener listener da associare al pulsante "Invia Richiesta"
     */
    public void addInviaListener(ActionListener listener) {
        inviarichiesta.addActionListener(listener);
    }

    /**
     * Collega un listener al pulsante per annullare l'operazione.
     * Il listener viene normalmente fornito dal controller.
     *
     * @param listener listener da associare al pulsante "Annulla"
     */
    public void addAnnullaListener(ActionListener listener) {
        annulla.addActionListener(listener);
    }

    /*
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    @SuppressWarnings("java:S100")
    private void $$$setupUI$$$() {
        mainPanel = new JPanel();
        mainPanel.setLayout(new GridLayoutManager(1, 1, new Insets(0, 0, 0, 0), -1, -1));
    }

    /*
     * @noinspection ALL
     */
    @SuppressWarnings("java:S100")
    public JComponent $$$getRootComponent$$$() {
        return mainPanel;
    }
}